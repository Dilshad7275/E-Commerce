@model MyApp.Models.ViewModel.CategoryVM
@{
    ViewBag.Title = "Index";

}

<div class="container py-4">
    <div class="row p-3">
        <div class="col-6">
           <h2 class="text-primary">Categories List</h2> 
        </div>
         <div class="col-6 text-end">
             <a class="btn btn-primary" asp-action="CreateUpdate" asp-controller="Category"><i class="bi bi-plus-square"> &nbsp;&nbsp;Create Category</i></a>
        </div>
    </div>
</div>
<table id="myTable" class="table table-border table-striped">
    <thead>
        <tr>
            <th>
                Name
            </th>
            <th>
                Display Order
            </th>
        </tr>
    </thead>
  @*  <tbody>
        @foreach (var item in Model.categories)
        {
            <tr>
                <td> @item.Name</td>
                <td> @item.DisplayOrder</td>
                <td role="group">
                    <a asp-action="CreateUpdate" asp-controller="Category" asp-route-id="@item.Id"><i class="bi bi-pencil-square"></i></a>
                    <a asp-action="Delete" asp-controller="Category" onclick="return confirm('Are you sure you want to delete this?')" asp-route-id="@item.Id"><i class="bi bi-trash3-fill"></i></a>
                </td>
            </tr>

        }
    </tbody>*@
</table>
@section scripts{
    <script src="~/js/Category.js"></script>
}